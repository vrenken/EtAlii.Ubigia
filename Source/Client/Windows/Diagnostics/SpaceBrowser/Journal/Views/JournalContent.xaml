<UserControl
    x:Class="EtAlii.Ubigia.Windows.Diagnostics.SpaceBrowser.JournalContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shared="clr-namespace:EtAlii.Ubigia.Windows;assembly=EtAlii.Ubigia.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid 
        DataContext="{Binding JournalViewModel}"
        BorderThickness="0" 
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        ScrollViewer.HorizontalScrollBarVisibility="Auto"
        ItemsSource="{Binding Items}"
        HeadersVisibility="Column"
        SelectionMode="Single">
        <i:Interaction.Behaviors>
            <shared:AutoScrollToLastItemBehavior/>
        </i:Interaction.Behaviors>
        <DataGrid.Columns>
            <DataGridTextColumn 
                Header="Id" 
                Binding="{Binding Id}" 
                Width="Auto"
                MinWidth="30"/>
            <DataGridTextColumn 
                Header="Moment" 
                Binding="{Binding Moment}" 
                Width="Auto" 
                MinWidth="140"/>
            <DataGridTextColumn 
                Header="Action" 
                Binding="{Binding Action}" 
                Width="Auto"
                MinWidth="90"/>
            <DataGridTextColumn 
                Header="Description" 
                Binding="{Binding Description}" 
                Width="*"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
