<tb:TaskbarIcon 
    x:Class="EtAlii.Ubigia.Windows.Tools.MediaImport.TaskbarIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:tb="clr-namespace:Hardcodet.Wpf.TaskbarNotification;assembly=Hardcodet.Wpf.TaskbarNotification"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    IconSource="{Binding IconToShow}" 
    ToolTipText ="{Binding ToolTipText}">
    
    <tb:TaskbarIcon.ContextMenu>
        <ContextMenu>
            <MenuItem 
                Header="Configuration" 
                IsEnabled="{Binding CanShowConfiguration}"
                Command="{Binding ShowConfigurationCommand}" />

            <MenuItem 
                Header="Status" 
                IsEnabled="{Binding CanShowStatus}"
                Command="{Binding ShowStatusCommand}" />
            <MenuItem 
                Header="About" 
                IsEnabled="{Binding CanShowAbout}"
                Command="{Binding ShowAboutCommand}" />
            <Separator />
            <MenuItem 
                Header="Exit" 
                Command="{Binding ExitApplicationCommand}" />
        </ContextMenu>
    </tb:TaskbarIcon.ContextMenu>
</tb:TaskbarIcon>
