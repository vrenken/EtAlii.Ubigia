# Clear the cache.
clr()

# Function call
$v1 <= testfunction()

#Get the full identifier for an item.
$id <= id(/Persons/Doe/John)
$id <= id($johnDoe)
$id <= id() <= /Persons/Doe/John
$id <= id() <= $johnDoe


#Output format function.
$contacts <= /Contacts/
<= format($contacts, "{item} {parent}")
<= format($contacts, "{item} [{branch}]") # add additional characters.

<= format("{item} {parent}") <= $contacts
<= format("{item} [{branch}]") <= $contacts # add additional characters.

<= view("Graph view 2") <= /Contacts/*/*/Emails/.IsPrimary=true # Output the nodes into the specified Space browser view.