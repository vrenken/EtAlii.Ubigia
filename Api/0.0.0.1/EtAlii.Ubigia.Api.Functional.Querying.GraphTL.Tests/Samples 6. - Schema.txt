-- ==================================================================================================================
-- 01 Add a schema.
@schema-add("contact.names") 
{
    FirstName @value(),
    LastName @value(\#FamilyName),
    NickName,
}