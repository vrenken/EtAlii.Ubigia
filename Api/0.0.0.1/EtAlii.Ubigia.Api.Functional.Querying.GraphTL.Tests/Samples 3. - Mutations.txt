-- ==================================================================================================================
-- 01.a Assign a property to an existing node. 
-- Throws an error if multiple nodes are found. 
Person @node(Person:Doe/John)
{
    NickName @value-assign("Johnny")
}

-- ==================================================================================================================
-- 01.b Assign a query property to a constant. 
Person @node(Person:Doe/John)
{
    NickName <= "Johnny"
}

-- ==================================================================================================================
-- 02. Clear a property on an existing node. 
-- Throws an error if multiple nodes are found. 
 Person @node(Person:Doe/John)
 {
    NickName @value-clear() 
 }

-- ==================================================================================================================
-- 03. Changes the name of an existing node. 
-- Throws an error if multiple nodes are found. 
 Person @node(Person:Doe/Johhn)
 {
     FirstName @value-assign("John")
 }


-- ==================================================================================================================
-- 04. Add a new node and give it some properties.  
 Person @node-add(Person:Doe, Jane)
 {
     NickName @value-assign("Jahney")
 }
 
-- ==================================================================================================================
-- 05.a Add a child node and return everything.
 Person @node(Person:Doe/John)
 {
     FirstName @value(),
     LastName @value(\#FamilyName),
     NickName,
     Friend @nodes-link(/Friends, Person:Stark/Tony, /Friends)
     {
        FirstName @value(),
        LastName @value(\#FamilyName),
        NickName
     }  
 }

-- ==================================================================================================================
-- 05.b Add a child node and return everything.
 Data
 {
     Person @node(Person:Doe/John)
     {
         FirstName @value(),
         LastName @value(\#FamilyName),
         NickName,
         Friend @nodes-link(/Friends, Person:Vrenken/Peter, /Friends)
         {
            FirstName @value(),
            LastName @value(\#FamilyName),
            NickName
         }  
     }
 }

-- ==================================================================================================================
-- 06 Link two nodes and return everything.
 Data
 {
     Person @node(Person:Doe/John)
     {
         FirstName @value(),
         LastName @value(\#FamilyName),
         NickName,
         Friend @nodes-link(/Friends, Person:Vrenken/Peter, /Friends)
         {
            FirstName @value(),
            LastName @value(\#FamilyName),
            NickName
         }  
     },
     Person @node(Person:Vrenken/Peter)
     {
         FirstName @value(),
         LastName @value(\#FamilyName),
         NickName,
         Friend @nodes(/Friends)
         {
            FirstName @value(),
            LastName @value(\#FamilyName),
            NickName
         }  
     }
 }

