namespace EtAlii.Servus.Api.Data
{
    using System.IO;

    public class ChecksumCalculator
    {
        public ulong Calculate(Stream stream)
        {
            //const bufferSize[] = 
            //stream.Position = 0;
            //while(stream.Position < stream.Length)
            //{

            //}
            return 0;
        }
    }
}