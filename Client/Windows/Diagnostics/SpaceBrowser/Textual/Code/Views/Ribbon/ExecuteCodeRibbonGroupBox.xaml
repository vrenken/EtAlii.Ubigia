<Fluent:RibbonGroupBox 
    x:Class="EtAlii.Ubigia.Client.Windows.Diagnostics.ExecuteCodeRibbonGroupBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
    xmlns:s="clr-namespace:System;assembly=mscorlib"  
    xmlns:Fluent="clr-namespace:Fluent;assembly=Fluent"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300"
    Fluent:KeyTip.Keys="C"
    Header="Execution">
    <Fluent:Button 
        Command="{Binding ExecuteCommand}" 
        IsEnabled="{Binding CanExecute, FallbackValue=false}"
        Header="Start" 
        LargeIcon="\Images\Icons\Media-Play.png" />
    <Fluent:Button 
        Command="{Binding PauseCommand}" 
        IsEnabled="{Binding CanStop, FallbackValue=false}"
        Header="Pause" 
        LargeIcon="\Images\Icons\Media-Pause.png" />
    <Fluent:Button 
        Command="{Binding StopCommand}" 
        IsEnabled="{Binding CanStop, FallbackValue=false}"
        Header="Stop" 
        LargeIcon="\Images\Icons\Media-Stop.png" />
    <Fluent:Button 
        Command="{x:Null}" 
        IsEnabled="False"
        Header="Analyse" 
        LargeIcon="\Images\Icons\Search-Find.png" />
</Fluent:RibbonGroupBox>