<UserControl
    x:Class="EtAlii.Servus.Client.Windows.Diagnostics.RootsContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
    xmlns:s="clr-namespace:System;assembly=mscorlib"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300" >
    <ListBox x:Name="listBox" 
        DataContext="{Binding RootsViewModel}"
        ItemsSource="{Binding AvailableRoots}"
        SelectedItem="{Binding SelectedRoot}"
        DisplayMemberPath="Name">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="SelectionChanged">
                <i:InvokeCommandAction Command="{Binding BeginEntryDragCommand}" CommandParameter="{Binding ElementName=listBox}" />
            </i:EventTrigger>
            <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                <i:InvokeCommandAction Command="{Binding BeginEntryDragCommand}" CommandParameter="{Binding ElementName=listBox}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </ListBox>
</UserControl>
