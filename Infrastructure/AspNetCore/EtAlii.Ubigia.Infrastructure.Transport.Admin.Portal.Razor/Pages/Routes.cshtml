@page
@model RoutesModel
@{
    ViewData["Title"] = "Routes";
}

<h2>@ViewData["Title"]</h2>
<h3>Route Debug Info</h3>

<!-- /* //NOSONAR */ --><table class="table table-striped table-bordered">
    <thead>
    <tr>
        <!-- /* //NOSONAR */ --><th>Route Template</th>
        <!-- /* //NOSONAR */ --><th>Controller</th>
        <!-- /* //NOSONAR */ --><th>Action</th>
        <!-- /* //NOSONAR */ --><th>Constraints/Verbs</th>
        <!-- /* //NOSONAR */ --><th>Name</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var route in Model.Routes)
    {
        @if (!string.IsNullOrEmpty(route.Template))
         {
             <tr>
                 <td>@route.Template</td>
                 <td>@route.Controller</td>
                 <td>@route.Action</td>
                 <td>@route.Constraint</td>
                 <td>@route.Name</td>
             </tr>
         }
    }
    </tbody>
</table>